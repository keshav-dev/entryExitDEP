import { AppBar, Toolbar } from '@material-ui/core'
import React from 'react'
import './Header.css'

const Header = () => {
  return (
    <header>
      <AppBar>
        <Toolbar>IIT Ropar Entry-Exit Portal</Toolbar>
      </AppBar>
    </header>
  )
}

export default Header